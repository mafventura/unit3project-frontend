import React from 'react'
import { Button, Card } from 'react-bootstrap'

export default function QuicksModal({water, sleep, mood, quote}) {
  const classNames = []




  return (
    <Card className={classNames.join(" ")}>
            <Card.Body>
    
            </Card.Body>

    </Card>
    
 )
}
